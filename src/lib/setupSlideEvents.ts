/** Fired by `SlideShell` when the user tries to advance from the setup slide (→, space, backdrop). */
export const SETUP_SLIDE_NEXT_EVENT = 'synth-setup-slide-next'

/** Fired by `SlideShell` when the user tries to advance from the product demo slide. */
export const PRODUCT_DEMO_SLIDE_NEXT_EVENT = 'synth-product-demo-slide-next'
