/** Total slides in the primary deck (main + appendix tail). */
export const DECK_SLIDE_TOTAL = 24

/** Main story slides (through Thank You) — used for page counters. */
export const MAIN_DECK_SLIDE_TOTAL = 10

/** Appendix slides appended after Thank You in the primary deck. */
export const APPENDIX_MAIN_TOTAL = 14

/** Draft / appendix deck (`#appendix`) — `AppendixDeck.tsx`. */
export const APPENDIX_SLIDE_TOTAL = 9
